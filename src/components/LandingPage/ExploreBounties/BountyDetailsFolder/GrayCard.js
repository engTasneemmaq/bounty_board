import React from "react";

const GrayCard = ({text}) => {
  return (
    <div className="rounded-[3px] bg-[#EFEFEF] px-4 py-2 text-center  ">
      {text}
    </div>
  );
};

export default GrayCard;
